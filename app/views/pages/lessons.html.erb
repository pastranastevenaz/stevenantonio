  <div class="jumbotron">
    <div class="container">
    <% if user_signed_in? %>
    <%= render 'lessons/home' %>
    <% else %>
      <h1>Welcome!</h1>
      <p>Register to get started.</p>
      <p>
        <%= link_to 'Register', new_user_registration_path, class: 'btn btn-primary btn-lg' %>
      </p>
    <% end %>
    </div>
  </div>